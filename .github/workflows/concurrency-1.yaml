name: Concurrency 1

run-name: Concurrency 1

on:
    workflow_dispatch:
        inputs:
            environment:
                description: "Environment"
                type: environment
                required: true

concurrency: 
    group: concurrency-1
    cancel-in-progress: true
jobs:
    sleep:
        name: Sleep
        runs-on: ubuntu-latest
        steps:
            - name: Sleep
              run: sleep 30